import React, { Component } from 'react';

export class Search extends Component {
  render() {
    return (
      <form>
        <input type="search" />
      </form>
    );
  }
}

export default Search;
